<form method="post" action="{{ path('app_book_library_delete', {library: book_library.library.id,'id': book_library.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_library.id) }}">
    <button class="btn">Delete</button>
</form>
