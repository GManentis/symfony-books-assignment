{% extends 'base.html.twig' %}

{% block title %}BookLibrary{% endblock %}

{% block body %}
    <h1>BookLibrary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_library.id }}</td>
            </tr>
            <tr>
                <th>Book</th>
                <td>{{ book_library.book.title }}</td>
            </tr>
            <tr>
                <th>Rented</th>
                <td>{{ book_library.rented ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book_library_index', { library: book_library.library.id }) }}">back to list</a>

    <a href="{{ path('app_book_library_edit', {library: book_library.library.id ,'id': book_library.id}) }}">edit</a>

    {{ include('book_library/_delete_form.html.twig') }}
{% endblock %}
