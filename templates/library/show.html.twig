{% extends 'base.html.twig' %}

{% block title %}Library{% endblock %}

{% block body %}
    <h1>Library</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ library.id }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ library.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ library.address }}</td>
            </tr>
            <tr>
                <th>SupervisorFirstname</th>
                <td>{{ library.supervisorFirstname }}</td>
            </tr>
            <tr>
                <th>SupervisorLastname</th>
                <td>{{ library.supervisorLastname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_library_index') }}">back to list</a>

    <a href="{{ path('app_library_edit', {'id': library.id}) }}">edit</a>

    {{ include('library/_delete_form.html.twig') }}
{% endblock %}
